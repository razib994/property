import React from 'react';
import Layout from './master/Layout';

const Registration = () => {
    return (
        <div>
            <h2>ddd</h2>
        </div>
    );
}
Registration.layout = (page) => <Layout title="Property Owner" children={page} />;

export default Registration;
